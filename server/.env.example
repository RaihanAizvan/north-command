# Server configuration example
# Copy to server/.env and adjust as needed.

# HTTP
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Database
MONGODB_URI=mongodb://localhost:27017/north_command

# Auth
# Use a long, random value in real deployments.
JWT_SECRET=change-me
JWT_EXPIRES_IN=12h

# AI (optional)
# Set this in your environment or secret manager; never commit a real key.
GEMINI_API_KEY=

# Choose a model returned by GET /api/ai/models in your environment.
# Examples (depending on your key): gemini-2.5-flash, gemini-2.0-flash
GEMINI_MODEL=gemini-2.5-flash

# Google Generative Language API version (v1beta recommended for broad model support)
GEMINI_API_VERSION=v1beta
# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
